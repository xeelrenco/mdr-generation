# Configurazione MDR Generator v1

# MotherDuck (Steps 2-4)
MOTHERDUCK_TOKEN=
MOTHERDUCK_DB=my_db

# --- Scope PDF (comune) ---
SOW_DIR=input/SoW
SCOPE_MAX_PDF_MB=32

# --- Pass 1: estrazione scope (Step 1 + pair recovery Step 2b) ---
# Provider dedotto dal nome modello (gpt→openai, gemini→gemini, claude→claude)
SCOPE_PASS1_LLM_MODEL=gpt-5.5
SCOPE_PASS1_CHUNK_ENABLED=true
SCOPE_PASS1_CHUNK_PAGES=10
SCOPE_PASS1_CHUNK_OVERLAP=1
# Re-pass solo pass 1: seconda chiamata LLM sui chunk con 0 segnali al 1° pass
SCOPE_PASS1_CHUNK_REPASS_ENABLED=true
SCOPE_PASS1_CHUNK_REPASS_MIN_CHARS=200

# --- Pass 2: gap mirato su coppie Renco mancanti (Step 2c) ---
# Nessun re-pass su pass 2 — una chiamata LLM per chunk
SCOPE_PASS2_ENABLED=true
SCOPE_PASS2_LLM_MODEL=gemini-2.5-pro
SCOPE_PASS2_MAX_PAIRS=60
SCOPE_PASS2_CHUNK_ENABLED=true
SCOPE_PASS2_CHUNK_PAGES=10
SCOPE_PASS2_CHUNK_OVERLAP=2

# --- Credenziali provider (fallback se SCOPE_PASS1/2_LLM_MODEL vuoto) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-6
CLAUDE_MAX_TOKENS=16384

VERTEX_CREDENTIALS_PATH=
VERTEX_PROJECT_ID=
VERTEX_LOCATION=europe-west1
GEMINI_MODEL=gemini-2.5-flash

PROJECT_CODE=7350
