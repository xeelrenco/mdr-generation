# Configurazione MDR Generator v1

# MotherDuck (Steps 2-4)
MOTHERDUCK_TOKEN=
MOTHERDUCK_DB=my_db

# Scope of Work PDF (default: tutti i .pdf in input/SoW/)
SOW_DIR=input/SoW

# Step 1 — PDF inviato direttamente all'LLM
SCOPE_LLM_PROVIDER=openai
SCOPE_MAX_PDF_MB=32

# Chunking PDF (più chiamate LLM per copertura; false = un PDF intero come prima)
SCOPE_CHUNK_ENABLED=false
SCOPE_CHUNK_PAGES=15
SCOPE_CHUNK_OVERLAP=0
# Secondo pass LLM sui chunk che al 1° pass restituiscono 0 segnali (solo con chunking attivo)
SCOPE_CHUNK_REPASS_ENABLED=true
# Salta re-pass se il testo estraibile nel chunk e >0 ma sotto soglia (0 = PDF scansionato → re-pass ok)
SCOPE_CHUNK_REPASS_MIN_CHARS=200

# OpenAI (modello con supporto PDF, es. gpt-4o)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Vertex Gemini (alternativa)
VERTEX_CREDENTIALS_PATH=
VERTEX_PROJECT_ID=
VERTEX_LOCATION=europe-west1
GEMINI_MODEL=gemini-2.5-flash

PROJECT_CODE=7350
